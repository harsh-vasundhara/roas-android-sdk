# ROASSensor Android SDK — Gradle project settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.caching=true
# AndroidX is the modern support library namespace (AGP 8 default; explicit here
# so the build never depends on the default).
android.useAndroidX=true
kotlin.code.style=official
