// Root build file for the ROASSensor Android SDK.
//
// The single library module (:roas) declares its own plugin versions inline, so
// there is nothing to configure here — this file exists so the project opens as
// a standard multi-module Gradle build in Android Studio. On import, Android
// Studio generates the Gradle wrapper (gradlew + gradle/wrapper/*); or run
// `gradle wrapper` once if you have a system Gradle.
